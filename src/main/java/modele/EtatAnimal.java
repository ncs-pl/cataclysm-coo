package modele;

public enum EtatAnimal {


    /** L'animal est affamé. */
    AFFAME,
    /** L'animal est rassasié. */
    RASSASIE,
    /** L'animal est devenu ami du joueur. */
    AMI,
    /** L'animal est junkie. */
    JUNKIE,
    /** L'animal est perché dans un arbre. */
    PERCHE_ARBRE,
    /** L'animal est caché dans un buisson. */
    CACHE_BUISSON,
}
